{
  "hosting": {
    "public": "artifacts/thea-portfolio/dist",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ],
    "scripts": {
    "deploy": "npm --prefix artifacts/thea-portfolio run build && firebase deploy --only hosting"
  }

  }
  
}
